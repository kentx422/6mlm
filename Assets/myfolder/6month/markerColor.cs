using UnityEngine;
using System.Collections;

public class markerColor : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//Color gray = new Color (0.1f, 0.1f, 0.1f, 1.0f);
		/*if (Input.GetKeyDown(KeyCode.D))	renderer.material.color = Color.black;
		if (Input.GetKeyDown(KeyCode.W))	renderer.material.color = Color.white;
		if (Input.GetKeyDown(KeyCode.R))	renderer.material.color = Color.red;
		if (Input.GetKeyDown(KeyCode.G))	renderer.material.color = Color.green;
		if (Input.GetKeyDown(KeyCode.B))	renderer.material.color = Color.blue;
		if (Input.GetKeyDown(KeyCode.Y))	renderer.material.color = Color.yellow;*/
	}
}
