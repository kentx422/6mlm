using UnityEngine;
using System.Collections;

public class testcc : MonoBehaviour {
	Color cc	 = new Color (1.0f, 1.0f, 0.0f, 0.8f);
	// Use this for initialization
	void Start () {
		//this.renderer.material.color = cc;
	}
	
	// Update is called once per frame
	void Update () {
		//print (this.renderer.material.color);
	}
}
